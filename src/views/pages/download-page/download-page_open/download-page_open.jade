.open-section(data-js-open)
  .section-container
    .instruction
      .helper-padding
        h2 3. Open Report Portal in new browser tab and login
        .step
          p.main-text.desktop Open new browser tab with
            a(href='localhost:8080',target='_blank') Report Portal.
          p.main-text.mobile Open new browser tab with Report Portal on local host.
          .step-note.first.mobile
            p If you deploy ReportPortal on a seperate host, use&nbsp;
              span http://IP_ADDRESS:8080.
          .access
            p.main-text For user access:
            .user
              .input.login
                span default
                span.desktop.copy(data-copy='default') Copy
              .input.password
                span 1q2w3e
                span.desktop.copy(data-copy='1q2w3e') Copy
        .step-note.first.desktop
          p If you deploy ReportPortal on a seperate host, use&nbsp;
            span http://IP_ADDRESS:8080.
        .step
          .access
            p.main-text For admin access:
            .user
              .input.login
                span superadmin
                span.desktop.copy(data-copy='admin') Copy
              .input.password
                span erebus
                span.desktop.copy(data-copy='erebus') Copy
        .step-note.warn
          p Please change admin password for security.
